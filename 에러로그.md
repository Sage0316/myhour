# 에러로그

AI와 함께 하꾸를 만들면서 **AI가 틀렸고 내가 잡아낸 것**을 남긴 기록이다.

용도는 두 가지다.

1. 같은 실수를 반복하지 않기 위한 체크리스트
2. "AI에 끌려간 것이 아니라 AI를 부렸다"는 증거 — 결과물만으로는 증명되지 않는 부분

각 항목에 **현업 표현**을 같이 적었다. 내가 이미 하고 있던 판단에 업계에서 쓰는 이름을
붙여두는 것이 목적이다. 용어 뜻은 맨 아래 [용어 정리](#용어-정리)에 모아뒀다.

## 원칙

- **실제로 잡은 것만 쓴다.** 부풀리면 이 문서의 유일한 가치인 신뢰가 없어진다.
- **누가 잡았는지 표시한다.** 내가 잡은 것과 AI가 스스로 찾은 것을 섞지 않는다.
- **근거를 남긴다.** 파일명·커밋. "그때 그랬다"는 기억은 증거가 아니다.

## 기록 형식

```
사건 한 줄 (관찰된 사실만)
→ 관찰: 실제로 무엇이었는가
→ 내가 한 판단: 내가 한 사고를 우리말로
→ 현업 표현: 그 사고의 업계 용어
→ 근거: 파일·커밋
```

날짜는 `myhour/CLAUDE.md`에 기록이 남은 것만 적었다. 나머지는 `날짜 미기록`으로 두고,
앞으로는 그날 바로 적는다.

---

## 사례

### 1. 무드 칩에 부정 감정이 하나도 없었음 (날짜 미기록)

→ **관찰**: AI 잘못이 아니라 내가 처음 준 목록 자체에 긍정만 있었음. AI는 그 결함을 지적하지 않음  
→ **내가 한 판단**: 만들어진 것이 아니라 **빠진 것**을 봄. 스펙 자체를 의심  
→ **현업 표현**: `요구사항 갭 분석` · `해피 패스 편향 식별` · `도메인 커버리지 검토`  
→ **근거**: `myhour/src/store.ts` MOOD_LIST 8종(슬픔·짜증·지침 추가), `llmDirector.ts` BGM `sad` 풀 신설  
→ **잡은 사람**: 나

### 2. 기록은 "치과 무섭다"인데 마무리 자막이 "이런 하루가 뿌듯해"로 나옴 (날짜 미기록)

→ **관찰**: AI 호출이 실패했는데 예외가 삼켜지고 기본 문구가 대신 나감. 화면상으로는 성공처럼 보였음  
→ **내가 한 판단**: 출력이 나온 것과 성공한 것을 구분. 결과가 입력과 무관하다는 점을 근거로 실패를 추정  
→ **현업 표현**: `사일런트 페일러 탐지` · `폴백 동작 확인` · `예외 삼킴 식별`  
→ **근거**: `llmDirector.ts` 에러 평탄화(`errMessage`) — 워커 에러가 `[object Object]`로 새지 않게 수정  
→ **잡은 사람**: 나

### 3. 로고 색을 "먹색"이라고 설명했지만 화면에서는 갈색으로 보임 (2026-07-22)

→ **관찰**: 색상값(`#1A1A1A`)은 의도대로였지만, 크림 배경과의 동시대비로 따뜻한 회색이 갈색으로 읽힘  
→ **내가 한 판단**: 명세값과 실제로 지각되는 결과를 분리. 설명이 아니라 화면을 기준으로 판정  
→ **현업 표현**: `비주얼 QA` · `스펙값 대비 렌더 결과 검증` · `동시대비 고려`  
→ **근거**: 순수 검정 `#000000`으로 교체. `myhour/CLAUDE.md`에 "`#1A1A1A` 같은 따뜻한 회색 금지" 명시  
→ **잡은 사람**: 나

### 4. 막대 몇 개만 색을 바꿔달라고 했는데 전체가 바뀜 (2026-07-22)

→ **관찰**: AI가 범위를 확인하지 않고 가장 실행하기 쉬운 해석을 택함. 보고는 "요청대로 완료"였음  
→ **내가 한 판단**: 지시 범위와 실행 범위의 불일치를 검출. 완료 보고를 결과로 인정하지 않음  
→ **현업 표현**: `스코프 크립 차단` · `변경 영향 범위 확인` · `수용 기준 재확인`  
→ **근거**: `myhour/public/brand/hakku-icon-mark.svg` — 글자당 한 획씩만 하늘색  
→ **잡은 사람**: 나

### 5. AI가 없는 "재생성" 기능을 있다고 설명함 (2026-07-22)

→ **관찰**: 코드에 존재하지 않는 기능이었음. 있을 법한 것을 있다고 단정한 환각  
→ **내가 한 판단**: 실제 제품과 설명의 불일치 검출  
→ **현업 표현**: `할루시네이션 검증` · `스펙 정합성 확인` · `실물 기준 검증`  
→ **근거**: 지적 후 실제 구현 — `ArchiveScreen.tsx` "다시 만들기" 버튼  
→ **잡은 사람**: 나

### 6. 합의한 적 없는 주황색을 "하꾸의 시그니처 컬러"라고 부름 (2026-07-22)

→ **관찰**: `scenes.ts`의 `drawMemeScene` 기본 인자값일 뿐. 코드에 자주 보인다는 이유로 AI가 브랜드 결정으로 승격시킴  
→ **내가 한 판단**: 합의된 결정과 코드에 우연히 존재하는 값을 구분. AI가 이름 붙인 것의 출처를 추적  
→ **현업 표현**: `결정 근거 추적(provenance)` · `암묵적 전제 검증` · `단일 진실 공급원 부재 식별`  
→ **근거**: `myhour/CLAUDE.md` — "주황(`#D9743F`)은 시그니처 컬러 아님" 명시  
→ **잡은 사람**: 나

### 7. 화면을 길게 누르면 문자열 선택으로 인식됨 (2026-07-22)

→ **관찰**: 저장 로직 문제가 아니라 iOS Live Text 동작. 영상 프레임에 박힌 글자를 텍스트로 인식해 롱프레스를 가로챔  
→ **내가 한 판단**: 현상 기반 원인 분리  
→ **현업 표현**: `재현 조건 식별` · `루트 코즈 분석`  
→ **근거**: `ArchiveScreen.tsx` `-webkit-touch-callout: none`  
→ **잡은 사람**: 나

### 8. 에어드랍하면 파일 앱에는 들어가는데 사진첩에는 안 들어감 (2026-07-22)

→ **관찰**: 저장 실패가 아니라 컨테이너 포맷 문제. iOS 사진 앱이 webm을 영상으로 인식하지 못함. **영상 저장 실패의 진짜 원인이 이것이었음**  
→ **내가 한 판단**: 내가 관찰한 사실("파일로는 되는데 사진첩은 안 된다")로 실패 지점을 좁힘. AI의 추측보다 관찰이 정확했음  
→ **현업 표현**: `플랫폼 호환성 이슈 격리` · `컨테이너와 코덱 구분` · `환경별 재현 테스트`  
→ **근거**: `videoGenerator.ts` mp4 우선순위. `myhour/CLAUDE.md`에 "절대 webm을 앞으로 올리지 말 것"  
→ **잡은 사람**: 나

### 9. 같은 증상에 AI가 세 번 연속 다른 원인을 짚고 세 번 실패함 (2026-07-22)

→ **관찰**: 버튼 없음 → `await`가 제스처 신뢰를 깸 → 오버레이가 터치를 가로챔. 세 번 다 틀림  
→ **내가 한 판단**: 수정 반복을 멈추게 하고 증상을 다시 기술함. 고치기가 아니라 문제 정의로 되돌림  
→ **현업 표현**: `가설 검증 루프 중단` · `문제 재정의(reframing)` · `디버깅 전략 전환`  
→ **근거**: 이 판단 직후 사례 8의 진짜 원인이 나옴  
→ **잡은 사람**: 나

### 10. 신발 사진에 💍가 붙는 식으로 이모지가 내용과 무관하게 나옴 (2026-07-25)

→ **관찰**: 하루 단위 무드 이모지 세트에서 장면마다 랜덤으로 뽑아 쓰고 있었음. 기록 단위 내용과 연결이 없었음  
→ **내가 한 판단**: 데이터 단위가 섞인 것을 발견. "랜덤"이 무엇의 랜덤인지 물음  
→ **현업 표현**: `입도(granularity) 불일치 식별` · `관심사 분리` · `결정성 확인`  
→ **근거**: `myhour/CLAUDE.md` "이모지 규칙 (2026-07-25 정리)" — 무드 이모지와 기록 이모지 분리  
→ **잡은 사람**: 나

### 11. AI가 근거 없이 내 작업을 "문제를 좁히지 못했다"고 평가함 (2026-07-25)

→ **관찰**: AI가 `aidlc-docs/inception`을 읽지 않고 코드만 보고 판정. 실제로는 목표 6개·비목표 5개·사용자 2종이 명시돼 있었고 평가가 뒤집힘  
→ **내가 한 판단**: 평가의 근거 범위를 물음. 결과물이 아니라 **판단 자체**를 검증 대상으로 삼음  
→ **현업 표현**: `근거 범위(evidence scope) 확인` · `표본 편향 식별` · `리뷰 커버리지 점검`  
→ **근거**: `aidlc-docs/inception/requirements/requirements.md` 3절 목표/비목표  
→ **잡은 사람**: 나

### 12. 병합 후 정상처럼 보였지만 이미 고친 버그 4개가 되살아남 (2026-07-25)

→ **관찰**: 파일을 통째로 main 것으로 가져오면서 기능이 빠지고 과거 수정이 되돌아감 — mp4/webm 순서 역전, BGM 파일명 3개 404, 사진이 다시 localStorage로, 아카이브 제목 유실. **테스트는 통과한 상태였음**  
→ **내가 한 판단**: 병합 결과를 신뢰하지 않고 확인을 지시("깃허브에 바뀐 내용 많으니까 한 번 봐봐")  
→ **현업 표현**: `리그레션 검증` · `테스트 커버리지 공백 식별` · `회귀 테스트 추가`  
→ **근거**: 커밋 `be48213`. 재발 방지 테스트 — `llmDirector.test.ts`(BGM 파일 존재 검사), `repositories/journalRepository.test.ts`(제목 왕복·짤 보존)  
→ **잡은 사람**: 나(확인 지시) + AI(원인 4개 특정)

### 13. 두 AI가 각자 다른 브랜치에서 작업해 같은 사고가 세 번 반복됨 (2026-07-25~26)

→ **관찰**: main과 작업 브랜치가 갈라진 상태로 둘이 병렬 작업. 여기서 세 사건이 파생됨 — ① 어제 회귀 4개 ② 코덱스가 `be48213`을 못 찾음 ③ 코덱스가 옛 기준으로 문서를 편집해 80줄이 날아갈 뻔함  
→ **내가 한 판단**: 세 사건을 개별 사고로 처리하지 않고 하나의 원인으로 묶음. 이후 "브랜치 쓰지 말고 main에 바로"로 작업 방식을 바꿈  
→ **현업 표현**: `브랜치 다이버전스` · `동기화 지점 부재` · `트렁크 기반 개발로 전환`  
→ **근거**: main과 브랜치를 `0ee6589`에서 동일하게 맞춤  
→ **잡은 사람**: 나

### 14. 문서에 기록된 내 결정이 승인 없이 뒤집혔고 문서는 그대로였음 (2026-07-26)

→ **관찰**: `myhour/CLAUDE.md` 8행에 "localStorage 키는 `myhour_*` 유지"라고 적혀 있는데, 실제 코드는 `hakku_*` 11개를 쓰고 있었음. 같은 파일 20·30행이 8행과 모순. `audit.md`에 승인 기록 없음. **AI들이 8행을 읽고 사실이라고 믿은 상태로 계속 작업했음**  
→ **내가 한 판단**: 배포 설명을 듣다가 "저장 방식이 바뀌었다"는 말에서 내 결정과의 불일치를 감지. 코드가 아니라 **결정 이력**을 따져 물음  
→ **현업 표현**: `문서-구현 드리프트 탐지` · `결정 기록(ADR) 부재` · `사후 승인 처리`  
→ **근거**: `myhour/CLAUDE.md` 8행 수정(변경 사실 + 승인 없이 들어갔음 + 유지 이유 명시), `aidlc-docs/audit.md`에 결정 기록 추가  
→ **잡은 사람**: 나

### 15. AI가 "키가 손상됐다"고 단정했지만 실제로는 잘 돌아가고 있었음 (2026-07-26)

→ **관찰**: AI가 `curl` 401 결과를 근거로 "워커의 API 키가 손상됐다, 새 키를 발급하라"고 단정. 나는 앱에서 AI 분석이 **정상 동작하는 걸 보고** 되물었음. 확인해보니 워커 키는 멀쩡했고, 그날 16:20에 실제로 성공한 기록이 KV에 남아 있었음  
→ **내가 한 판단**: AI의 진단을 그대로 받아들이지 않고 **내 눈으로 본 앱 동작**을 반증으로 들이댐. 키 재발급·세션 재시작·환경변수 설정을 다 하기 **전에** 물어봄  
→ **현업 표현**: `오진(misdiagnosis)` · `반증 사례로 가설 기각` · `관측 가능성 없는 대상을 추론으로 단정` · `불필요한 롤백 방지`  
→ **근거**: `AI_STATE` KV의 `quota:2026-07-26:*` = 1, 16:20 KST 기록. `myhour/CLAUDE.md`의 진단 절을 정반대로 수정  
→ **잡은 사람**: 나

**AI가 왜 틀렸는가 (구조를 알아두면 다음에 또 잡을 수 있다)**: 워커 시크릿은 **되읽을 수 없다.**
AI는 대화 요약본에서 키를 재구성해 찔러보고 401을 받았는데, 그건 "요약본에서 읽은 문자열이 무효"라는
증명일 뿐 워커에 올라간 문자열이 아니다. **관측할 수 없는 대상에 대해 간접 증거로 단정한 것**이 오류다.
→ 다음에 물어볼 것: **"그거 직접 본 거야, 추론한 거야?"**

그리고 이 오진이 가능했던 진짜 이유는 앱이 인증 실패·프로바이더 거부·출력 형식 오류를 전부
`analysis_unavailable` 하나로 보여주기 때문이다. **사례 2의 침묵 실패가 부른 두 번째 청구서다.**
오류를 구분해서 보여주면 오진할 여지 자체가 없어진다.
→ **현업 표현**: `오류 코드 세분화` · `관측 가능성(observability)` · `기술 부채의 이자`

**여기서 배운 것 하나 더**: 내가 "비용"이라고 말한 것과 실제 비용이 다를 수 있다.
저장소명·URL·워커명 변경은 진짜 비용이고 그건 지켜졌지만, localStorage 키 이름은 공짜였다.
배포가 무서운 이유는 접두사가 아니라 스키마 v2 마이그레이션이었다.
→ **현업 표현**: `변경 비용 산정` · `위험 원인 오귀속(misattribution)`

---

## 반복된 패턴 → 다음 작업 체크리스트

사례에서 규칙만 뽑았다. 새 기능을 받을 때마다 이걸 돌린다.

1. **"있다"고 하면 눌러본다.** 코드 설명이 아니라 실제 버튼으로. → `실물 기준 검증` (5, 12)
2. **AI가 이름 붙인 것은 출처를 묻는다.** "그거 어디서 정한 거야?" → `결정 근거 추적` (6, 11)
3. **같은 증상으로 두 번 실패하면 가설을 버린다.** 세 번째 수정 대신 증상을 다시 쓴다. → `문제 재정의` (9)
4. **색·문구·범위는 내 눈으로 본 것이 기준.** → `비주얼 QA`, `변경 영향 범위 확인` (3, 4)
5. **빠진 것을 본다.** 스펙에 없는 빈칸은 AI가 먼저 말해주지 않는다. → `요구사항 갭 분석` (1, 10)
6. **출력이 나와도 성공이 아니다.** 결과가 입력과 무관하면 폴백이 돌고 있다. → `사일런트 페일러 탐지` (2)
7. **병합 뒤에는 예전에 고친 버그를 다시 본다.** → `리그레션 검증` (12)
8. **부정적 평가도 근거를 묻는다.** AI는 자기 판단도 환각한다. → `근거 범위 확인` (11)
9. **작업선은 하나로 유지한다.** 브랜치가 갈라지면 사고가 반복된다. → `트렁크 기반 개발` (13)
10. **문서에 적힌 내 결정이 아직 사실인지 확인한다.** 뒤집혔는데 문서만 남아 있으면 모두가 틀린 전제로 일한다. → `문서-구현 드리프트 탐지` (14)

## 용어 정리

위에서 쓴 표현만 모았다. 면접에서 이 단어로 말하면 같은 판단이 훨씬 짧게 전달된다.

| 용어 | 뜻 | 내 사례 |
|---|---|---|
| **루트 코즈 분석** (RCA) | 증상이 아니라 근본 원인을 찾는 것. 증상만 고치면 다시 터진다 | 7, 8 |
| **재현 조건 식별** | "어떤 상황에서 반드시 재현되는가"를 특정하는 것. 이게 없으면 고쳤는지 확인도 불가 | 7 |
| **문제 재정의** (reframing) | 원인 가설이 계속 틀릴 때, 수정을 멈추고 문제 기술로 되돌아가는 것 | 9 |
| **할루시네이션** | AI가 없는 사실을 그럴듯하게 만들어내는 것 | 5, 6 |
| **스펙 정합성** | 문서에 쓴 것과 실제 구현이 일치하는가 | 5 |
| **실물 기준 검증** | 설명·문서가 아니라 실제 동작하는 것으로 확인 | 5, 12 |
| **사일런트 페일러** | 실패했는데 에러가 안 나고 성공처럼 보이는 것. 가장 잡기 어려운 유형 | 2 |
| **폴백** (fallback) | 주 경로가 실패했을 때 대신 쓰는 대안 경로 | 2 |
| **예외 삼킴** | 에러를 잡아놓고 아무 처리도 안 해서 조용히 넘어가는 것 | 2 |
| **해피 패스** | 모든 것이 정상인 경우의 흐름. 여기만 만들면 실제 사용에서 깨진다 | 1 |
| **요구사항 갭 분석** | 스펙에 빠진 것이 무엇인지 찾는 작업 | 1, 10 |
| **스코프 크립** | 요청 범위를 넘어 작업이 번지는 것 | 4 |
| **변경 영향 범위** | 이 변경이 실제로 건드리는 범위. 의도보다 넓은지 확인 | 4 |
| **수용 기준** | "이렇게 되면 완료"라고 미리 정한 조건 | 4 |
| **리그레션(회귀)** | 잘 되던 것이 다시 망가지는 것 | 12 |
| **회귀 테스트** | 같은 버그가 다시 나면 즉시 실패하도록 만든 테스트 | 12 |
| **테스트 커버리지 공백** | 테스트가 닿지 않는 영역. 회귀는 항상 여기서 난다 | 12 |
| **입도** (granularity) | 데이터를 다루는 단위. 하루 단위와 기록 단위를 섞으면 결과가 어긋난다 | 10 |
| **관심사 분리** | 성격이 다른 것을 한 덩어리로 처리하지 않는 것 | 10 |
| **결정성** (determinism) | 같은 입력이면 언제나 같은 출력. AI 판단은 비결정적이라 판정에 쓰면 안 된다 | 10 |
| **단일 진실 공급원** (SSOT) | 그 정보의 정본이 어디인지 하나로 정해두는 것. 없으면 코드에 흩어진 값이 결정처럼 굳는다 | 6 |
| **결정 근거 추적** (provenance) | 이 값이 누가 언제 왜 정한 것인지 거슬러 확인하는 것 | 6, 11 |
| **비주얼 QA** | 명세값이 아니라 실제 렌더된 화면으로 검증 | 3 |
| **동시대비** | 배경색에 따라 같은 색이 다르게 보이는 현상 | 3 |
| **컨테이너와 코덱** | 컨테이너=포장 형식(mp4, webm), 코덱=압축 방식(h264, vp9). 재생 가능 여부는 둘 다 봐야 한다 | 8 |
| **플랫폼 호환성** | 같은 코드가 기기·OS에 따라 다르게 동작하는 문제 | 8 |
| **근거 범위** (evidence scope) | 그 결론을 무엇을 보고 냈는가. 범위가 좁으면 결론이 틀린다 | 11 |
| **표본 편향** | 일부만 보고 전체를 판단해 생기는 왜곡 | 11 |
| **브랜치 다이버전스** | 작업선이 갈라져 서로의 변경이 안 보이는 상태 | 13 |
| **트렁크 기반 개발** | 브랜치를 오래 두지 않고 main 하나에서 자주 합치는 방식 | 13 |
| **ADR** (결정 기록) | "왜 이렇게 정했는지"를 남기는 문서. 없으면 결정이 조용히 뒤집힌다 | 14 |
| **문서-구현 드리프트** | 문서와 실제 코드가 시간이 지나며 어긋나는 것 | 14 |
| **위험 원인 오귀속** | 무서운 이유를 엉뚱한 곳에서 찾는 것. 접두사가 아니라 스키마가 위험이었다 | 14 |

## 아직 내 것이 아닌 것

이 문서의 신뢰를 지키기 위해 같이 남긴다.

- **AI/코드 역할 분담 설계는 내가 한 것이 아니다.** AI가 짰다.
  예외 하나 — 이모지를 고정 사전으로 둘지 AI 자유 판단에 맡길지 물어보고,
  트레이드오프를 듣고 **고정 사전을 선택**한 것은 내 결정이다.
  (현업 표현: `결정성 확보를 위한 규칙 고정`)
- **용어를 말로 설명하지 못한다.** IndexedDB가 무엇인지 몰랐다.
  판단은 되지만 설명이 안 되는 상태.

## 다음에 답할 수 있어야 하는 것

문서 안 보고 대답해보고, 틀린 곳을 AI에게 짚어달라고 한다.

1. 하꾸에서 사진·음성을 IndexedDB에 넣은 이유 — localStorage와 뭐가 다른지
2. mp4를 webm보다 먼저 두는 이유 — 어디서 어떻게 실패했는지
3. AI에 맡긴 것과 코드로 고정한 것의 경계 — 왜 그렇게 나눴는지

세 개를 내 말로 설명할 수 있으면, 도구가 무엇이었든 내 능력이다.
