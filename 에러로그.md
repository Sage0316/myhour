# 에러로그

AI와 함께 하꾸를 만들면서 **AI가 틀렸고 내가 잡아낸 것**을 남긴 기록이다.

두 가지 용도로 쓴다.

1. 같은 실수를 반복하지 않기 위한 체크리스트 (맨 아래 "반복된 패턴" 절)
2. "AI에 끌려간 것이 아니라 AI를 부렸다"는 증거 — 결과물만으로는 증명되지 않는 부분

## 원칙

- **실제로 잡은 것만 쓴다.** 그럴듯하게 부풀리면 이 문서의 유일한 가치인 신뢰가 없어진다.
- **누가 잡았는지 표시한다.** 내가 잡은 것과 AI가 스스로 찾은 것을 섞지 않는다.
- **근거를 남긴다.** 파일명·커밋·문서 위치. "그때 그랬다"는 기억은 증거가 아니다.

## 기록 형식

```
### N. 한 줄 제목 (날짜)
- 증상:
- AI가 한 말:
- 내가 짚은 것:
- 실제 원인:
- 규칙:        ← 다음에 같은 일을 막는 문장
- 근거:
- 잡은 사람:
```

날짜는 `myhour/CLAUDE.md`에 기록이 남은 것만 적었다. 나머지는 `날짜 미기록`으로 두고,
앞으로는 그날 바로 적는다.

---

## 사례

### 1. 스펙에 없는 빈칸은 AI가 보지 못한다 — 감정이 긍정 일색이었다 (날짜 미기록)

- **증상**: 무드 칩이 잔잔함·뿌듯함·감성·웃김·정신없음 5개뿐. 나쁜 하루를 기록할 칸이 없었다.
- **AI가 한 말**: 없음. 요청한 대로 5개를 만들고 끝냈다.
- **내가 짚은 것**: "너무 좋은 감정 밖에 없네."
- **실제 원인**: 내가 처음 준 목록이 긍정만이었고, AI는 그 목록의 결함을 지적하지 않았다.
- **규칙**: **AI는 내가 준 스펙 안에서만 옳다.** 스펙 자체가 틀렸는지는 내가 본다.
  특히 "빠진 것"은 AI가 절대 먼저 말해주지 않는다.
- **근거**: `myhour/src/store.ts` MOOD_LIST 8종 (슬픔·짜증·지침 추가),
  `llmDirector.ts` BGM `sad` 풀 신설
- **잡은 사람**: 나

### 2. AI 응답이 조용히 디폴트값으로 떨어졌다 (날짜 미기록)

- **증상**: 기록 내용은 "치과 무섭다"인데, 영상 마무리 자막에 "이런 하루가 뿌듯해" 류의
  기본 문구가 나왔다.
- **AI가 한 말**: 정상 동작으로 취급했다.
- **내가 짚은 것**: "되는 거 맞나? 존나 애매한데. 마지막에도 치과 무섭다니까 느닷없이
  이런 하루가 뿌듯해 어쩌구 디폴트값 나오고."
- **실제 원인**: AI 호출이 실패했는데 예외가 삼켜지고 폴백 문구가 대신 나갔다.
  화면상으로는 "성공"처럼 보였다.
- **규칙**: **조용한 실패를 의심한다.** 출력이 나온다고 성공이 아니다.
  결과가 입력과 무관해 보이면 폴백이 돌고 있는 것이다.
- **근거**: `llmDirector.ts` 에러 메시지 평탄화(`errMessage`),
  워커 에러를 `[object Object]`로 흘리지 않도록 수정
- **잡은 사람**: 나

### 3. 색을 "먹색"이라고 이름 붙였지만 실제로는 갈색이었다 (2026-07-22)

- **증상**: 로고를 `#1A1A1A`로 그렸다.
- **AI가 한 말**: 먹색, 부드러운 검정이라고 설명했다.
- **내가 짚은 것**: "이게 먹색이야? 똥색인지."
- **실제 원인**: 크림색 배경과의 동시대비로 따뜻한 회색이 갈색으로 읽혔다.
  색 이름은 맞았지만 **눈에 보이는 결과**가 틀렸다.
- **규칙**: **이름이 아니라 화면이 기준이다.** AI의 색·톤 설명은 검증 대상이지 결론이 아니다.
- **근거**: 순수 검정 `#000000`으로 교체. `myhour/CLAUDE.md` 로고 절에
  "절대 `#1A1A1A` 같은 따뜻한 회색 쓰지 말 것"으로 못 박음
- **잡은 사람**: 나

### 4. 지시 범위를 넓게 해석해서 전체를 바꿨다 (2026-07-22)

- **증상**: 로고에서 특정 막대 몇 개만 색을 바꿔달라고 했는데 전체 색을 바꿨다.
- **AI가 한 말**: 요청대로 바꿨다고 보고했다.
- **내가 짚은 것**: "아니 전체 다를 바꾸라는 게 아니라 내가 보내준 이미지처럼
  특정 막대기들만 바꿀 수 있냐고."
- **실제 원인**: AI가 범위를 확인하지 않고 가장 쉬운 해석으로 실행했다.
- **규칙**: **"바꿨다"는 보고를 믿지 말고 바뀐 범위를 본다.** 넓게 해석한 쪽이 편하기 때문에
  AI는 자주 넓게 해석한다.
- **근거**: `myhour/public/brand/hakku-icon-mark.svg` — 글자당 한 획씩만 하늘색
- **잡은 사람**: 나

### 5. 없는 기능을 있다고 말했다 (2026-07-22)

- **증상**: AI가 "재생성 기능으로 다시 만들 수 있다"고 안내했다.
- **AI가 한 말**: 그 기능이 이미 있다고 단정했다.
- **내가 짚은 것**: "재생성?? 그런 기능이 어딨어??"
- **실제 원인**: 코드에 존재하지 않는 기능이었다. 있을 법한 것을 있다고 말한 환각.
- **규칙**: **"있다"고 하면 화면에서 눌러본다.** 코드 설명이 아니라 실제 버튼으로 확인한다.
- **근거**: 지적 후 실제로 구현 — `ArchiveScreen.tsx` "다시 만들기" 버튼
- **잡은 사람**: 나

### 6. 정하지도 않은 것을 "우리가 정한 것"처럼 말했다 (2026-07-22)

- **증상**: AI가 주황색 `#D9743F`를 "하꾸의 시그니처 컬러"라고 부르며 로고 시안을 만들었다.
- **AI가 한 말**: 기존에 합의된 브랜드 컬러인 것처럼 전제했다.
- **내가 짚은 것**: "저 주황이 언제 시그니처 컬러였음? 난 허락한 적 없는데."
- **실제 원인**: `scenes.ts`의 `drawMemeScene` 기본 인자값일 뿐이었다.
  코드에 자주 보인다는 이유로 AI가 브랜드 결정으로 승격시켰다.
- **규칙**: **AI가 이름 붙인 것은 출처를 묻는다.** "그거 어디서 정한 거야?"
  코드에 있다는 것과 내가 결정했다는 것은 다르다.
- **근거**: `myhour/CLAUDE.md` — "**주황(#D9743F)은 시그니처 컬러 아님**" 명시
- **잡은 사람**: 나

### 7. 같은 증상을 세 번 연속 헛짚었고, 원인은 내가 찾았다 (2026-07-22)

- **증상**: 영상을 만들어도 사진첩에 저장되지 않았다. 길게 누르면 저장 대신
  화면의 글자가 선택됐다.
- **AI가 한 말**: 버튼이 없어서다 → `await`가 제스처 신뢰를 깨서다 →
  오버레이 버튼이 터치를 가로채서다. 세 번 고쳤고 세 번 실패했다.
- **내가 짚은 것**: "너가 지금 말 이해를 못하나본데. 화면을 길게 누르면
  화면에 있는 문자열을 선택한 걸로 인식된다고." / "공유할 수 없는 형식이래.
  아이패드로 에어드랍은 되는데 파일로 들어가고 사진첩에는 안 들어가."
- **실제 원인**: 두 개였다. (1) iOS Live Text가 영상 프레임에 박힌 글자를
  텍스트로 인식해 롱프레스를 가로챘다. (2) **진짜 원인은 코덱** — webm은 iOS 사진 앱이
  영상으로 인식하지 못해서 공유해도 파일 앱에만 들어갔다.
- **규칙**: **같은 증상으로 두 번 실패하면 원인 가설을 버린다.** 세 번째 수정은
  하지 말고 증상을 다시 기술한다. "파일로는 들어가는데 사진첩엔 안 들어간다"처럼
  **내가 관찰한 사실**이 AI의 추측보다 정확하다.
- **근거**: `videoGenerator.ts` mp4 우선순위, `ArchiveScreen.tsx`
  `-webkit-touch-callout: none`. `myhour/CLAUDE.md`에 "절대 webm을 앞으로 올리지 말 것"
- **잡은 사람**: 나 (원인 지목)

### 8. 이모지가 내용과 무관하게 붙었다 (2026-07-25)

- **증상**: 신발 사진에 💍가 붙는 식으로 장면마다 엉뚱한 이모지가 나왔다.
- **AI가 한 말**: 무드 이모지를 각 장면에 뿌리는 것으로 설계하고 정상 동작으로 봤다.
- **내가 짚은 것**: "화면마다 이모지 상관 없는 게 나오는데 이거 개선 못해?"
- **실제 원인**: 하루 단위 무드 이모지와 기록 단위 내용 이모지를 구분하지 않고,
  같은 세트에서 매번 랜덤으로 뽑아 썼다.
- **규칙**: **AI가 "랜덤으로 뽑는다"고 하면 무엇의 랜덤인지 확인한다.**
  단위가 섞이면 결과가 내용과 무관해진다.
- **근거**: `myhour/CLAUDE.md` "이모지 규칙 (2026-07-25 정리)" —
  무드 이모지와 기록 이모지를 절대 섞지 않기로 문서화
- **잡은 사람**: 나

### 9. AI가 근거 없이 내 작업을 낮게 평가했다 (2026-07-25)

- **증상**: AX 리포트 기준으로 하꾸를 평가하면서 "문제를 좁히지 못했다"고 단정했다.
- **AI가 한 말**: 예선 축 3번(문제 좁힘)이 약하다고 판정했다.
- **내가 짚은 것**: "내가 문제를 못 좁혔어?"
- **실제 원인**: **AI가 `aidlc-docs/inception`을 읽지 않고 코드만 보고 판단했다.**
  실제로는 목표 6개·비목표 5개·사용자 2종이 명시돼 있었다. 평가가 뒤집혔다.
- **규칙**: **AI의 평가도 환각할 수 있다.** 특히 부정적 평가는 근거 범위를 묻는다.
  "그거 뭘 보고 판단한 거야?" 한 문장이 평가를 뒤집었다.
- **근거**: `aidlc-docs/inception/requirements/requirements.md` 3절 목표/비목표
- **잡은 사람**: 나

### 10. 병합이 이미 고친 버그를 조용히 되돌렸다 (2026-07-25)

- **증상**: main 병합 후 겉으로는 정상이었다.
- **AI가 한 말**: 병합 완료, 테스트 통과.
- **내가 짚은 것**: "지금 깃허브에 바뀐 내용 많으니까 한 번 봐봐."
  — 확인을 지시한 것이 발견의 계기였다.
- **실제 원인**: 파일을 통째로 main 것으로 가져오면서 기능이 빠지고 **이미 고쳤던 버그
  4개가 되살아났다.** mp4/webm 순서 역전, BGM 파일명 3개 404, 사진이 다시
  localStorage로, 아카이브 제목 유실.
- **규칙**: **병합·리팩터 뒤에는 새 기능이 아니라 "예전에 고친 버그"를 다시 확인한다.**
  테스트 통과는 회귀가 없다는 뜻이 아니다 — 테스트가 없던 부분이 되돌아간다.
- **근거**: 커밋 `be48213`. 재발 방지로 테스트 추가:
  `llmDirector.test.ts`(BGM 파일 실제 존재 검사),
  `repositories/journalRepository.test.ts`(제목 왕복·짤 보존)
- **잡은 사람**: 나(확인 지시) + AI(구체적 원인 4개 특정)

---

## 반복된 패턴 → 다음 작업 체크리스트

사례에서 규칙만 뽑았다. 새 기능을 받을 때마다 이걸 돌린다.

1. **"있다"고 하면 눌러본다.** 코드 설명이 아니라 실제 버튼으로. (5, 10)
2. **AI가 이름 붙인 것은 출처를 묻는다.** "그거 어디서 정한 거야?" (6, 9)
3. **같은 증상으로 두 번 실패하면 가설을 버린다.** 세 번째 수정 대신 증상을 다시 쓴다. (7)
4. **색·문구·범위는 내 눈으로 본 것이 기준.** AI의 설명은 검증 대상. (3, 4)
5. **빠진 것을 본다.** 스펙에 없는 빈칸은 AI가 먼저 말해주지 않는다. (1, 8)
6. **출력이 나와도 성공이 아니다.** 결과가 입력과 무관하면 폴백이 돌고 있다. (2)
7. **병합 뒤에는 예전에 고친 버그를 다시 본다.** (10)
8. **부정적 평가도 근거를 묻는다.** AI는 자기 판단도 환각한다. (9)

## 아직 내 것이 아닌 것

이 문서의 신뢰를 지키기 위해 같이 남긴다.

- **AI/코드 역할 분담 설계는 내가 한 것이 아니다.** AI가 짰다.
  예외 하나: 이모지를 고정 사전으로 둘지 AI 자유 판단에 맡길지 물어보고,
  트레이드오프를 듣고 **고정 사전을 선택**한 것은 내 결정이다.
- **용어를 말로 설명하지 못한다.** IndexedDB가 무엇인지 몰랐다.
  판단은 되지만 설명이 안 되는 상태.

## 다음에 답할 수 있어야 하는 것

문서 안 보고 대답해보고, 틀린 곳을 AI에게 짚어달라고 한다.

1. 하꾸에서 사진·음성을 IndexedDB에 넣은 이유 — localStorage와 뭐가 다른지
2. mp4를 webm보다 먼저 두는 이유 — 어디서 어떻게 실패했는지
3. AI에 맡긴 것과 코드로 고정한 것의 경계 — 왜 그렇게 나눴는지

세 개를 내 말로 설명할 수 있으면, 도구가 무엇이었든 내 능력이다.
